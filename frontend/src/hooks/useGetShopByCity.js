/**
 * Stub hook — shop-by-city fetching was part of the old delivery-based
 * architecture and is not used in the current dine-in table-ordering flow.
 * Kept here so App.jsx imports resolve without errors.
 */
const useGetShopByCity = () => {
  // No-op in current architecture
};

export default useGetShopByCity;
